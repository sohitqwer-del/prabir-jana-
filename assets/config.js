/* ============================================================
   TRIBUTE PORTAL — connection settings
   ------------------------------------------------------------
   Leave these blank and the site stores praises/messages only
   in each visitor's own browser (great for previewing).

   To collect entries from EVERYONE in one shared place (free):
   1. Create a free project at https://supabase.com
   2. In the project: Settings → API
        - copy the "Project URL"        -> SUPABASE_URL
        - copy the "anon / public" key   -> SUPABASE_ANON_KEY
   3. Run the SQL in  setup/supabase.sql  (SQL Editor → paste → Run)
   4. Paste the two values below and re-deploy.
   ============================================================ */
window.TRIBUTE_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
